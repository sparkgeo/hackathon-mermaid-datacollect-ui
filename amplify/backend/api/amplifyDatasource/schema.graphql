type Status {
  id: ID!
  user: String!
  field: String!
  time: Int!
}

type Site @model {
  id: ID!
  name: String!
  country: String!
  latitude: Float!
  longitude: Float!
  exposure: String!
  reefType: String!
  reefZone: String!
  notes: String
  createdAt: String
  siteStatus: [Status]
}
